<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Jnana.Views.VoidPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Jnana.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Jnana.ViewModels"
    mc:Ignorable="d">

    <Grid Padding="24" Background="#0FFFFFFF">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Column="0" BorderBrush="DarkGray" BorderThickness="3" CornerRadius="8">
            <ItemsRepeater Background="{ThemeResource SystemControlAcrylicWindowBrush}" ItemsSource="{x:Bind ViewModel.Items}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemHeight="42" MinItemWidth="280" Orientation="Vertical"/>
                </ItemsRepeater.Layout>
            </ItemsRepeater>
        </ScrollViewer>
        <ScrollViewer Grid.Column="1" BorderBrush="DarkGray" BorderThickness="3" CornerRadius="8">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Items}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemHeight="42" MinItemWidth="280" Orientation="Vertical"/>
                </ItemsRepeater.Layout>
            </ItemsRepeater>
        </ScrollViewer>
        <ScrollViewer Grid.Column="2" BorderBrush="DarkGray" BorderThickness="3" CornerRadius="8">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Items}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemHeight="42" MinItemWidth="280" Orientation="Vertical"/>
                </ItemsRepeater.Layout>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
