<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Jnana.Views.ShellPage"
    xmlns:controls="using:Jnana.Vanara.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Jnana.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    mc:Ignorable="d">

    <Grid BorderThickness="3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid x:Name="DragableHeaderGrid" Grid.ColumnSpan="2" Grid.Row ="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- Vanara Logo Area 51 -->
            <Grid Grid.Column="0" Grid.Row="0" Grid.RowSpan="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- TODO: Add navigate back button -->
                <Image Grid.Column="0" Grid.Row="0" Height="96" HorizontalAlignment="Stretch" Source="/Assets/Vanara/Vanara-64px.png"/>
                <TextBlock Grid.Column="0" Grid.Row="1" FontSize="14" HorizontalAlignment="Center" Text="Vanara Jñāna"/>
            </Grid>
            <!-- Feature Tiles Area -->
            <StackPanel Grid.Column="1" Grid.Row="0" HorizontalAlignment="Stretch" Orientation="Horizontal" Padding="7,0">
                <controls:FeatureTile Command="{x:Bind NavigateCommand}" Icon="&#xE7B8;" Title="NuGets" Subtitle="Package Releases"/>
                <controls:FeatureTile Command="{x:Bind NavigateCommand}" Icon="&#xE9D5;" Title="GitHub" Subtitle="Vanara on GitHub"/>
                <controls:FeatureTile Command="{x:Bind NavigateCommand}" Icon="&#xE8F1;" Title="Samples" Subtitle="Examples and Unit Tests"/>
                <AppBarSeparator Margin="12,0"/>
                <controls:FeatureTile Command="{x:Bind NavigateCommand}" Icon="&#xEA86;" Title="Assemblies" Subtitle="Explore Types, Members and Interfaces"/>
                <controls:FeatureTile Command="{x:Bind NavigateCommand}" Icon="&#xE90F;" Title="Utilities" Subtitle="Tools, Helpers, Generators"/>
                <AppBarSeparator Margin="12,0"/>
                <controls:FeatureTile Command="{x:Bind NavigateCommand}" Icon="&#xE713;" Title="Settings" Subtitle="Settings, Version Info, Help"/>
            </StackPanel>
            <!-- Navigation Area -->
            <Grid Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Margin="12,8,12,4" Padding="7" VerticalAlignment="Stretch" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="BtnPrevious" Grid.Column="0" IsEnabled="False" ToolTipService.ToolTip="Previous" VerticalAlignment="Stretch">
                    <FontIcon Glyph="&#xE72B;"/>
                </Button>
                <SplitButton x:Name="BtnNext" Grid.Column="1" IsEnabled="False" ToolTipService.ToolTip="Next" VerticalAlignment="Stretch">
                    <SplitButton.Content>
                        <FontIcon Glyph="&#xE72A;"/>
                    </SplitButton.Content>
                    <SplitButton.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="NuGets"/>
                            <MenuFlyoutItem Text="GitHub"/>
                            <MenuFlyoutItem Text="Samples"/>
                            <MenuFlyoutItem Text="Assemlbies"/>
                            <MenuFlyoutItem Text="Utilities"/>
                        </MenuFlyout>
                    </SplitButton.Flyout>
                </SplitButton>
                <Button x:Name="UpToParentButton" Grid.Column="2" IsEnabled="False" Padding="0" ToolTipService.ToolTip="Navigate Up to Parent Folder" VerticalAlignment="Stretch" Width="36">
                    <SymbolIcon Symbol="Up" />
                </Button>
                <Border Grid.Column="3" Background="#F3F3F3" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="4" Height="32" VerticalAlignment="Stretch" >
                    <BreadcrumbBar x:Name="NavBreadcrumb" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsEnabled="False" ItemClicked="NavBreadcrumb_ItemClicked"/>
                </Border>
                <Button x:Name="ReloadButton" Grid.Column="4" Height="32" IsEnabled="False" ToolTipService.ToolTip="Reload View..." VerticalAlignment="Stretch">
                    <FontIcon Glyph="&#xE72C;"/>
                </Button>
                <AutoSuggestBox x:Name="SearchBox" Grid.Column="5" Height="32" IsEnabled="False" PlaceholderText="Share your Thoughts..." QueryIcon="Find" QuerySubmitted="SearchBox_QuerySubmitted" TextChanged="SearchBox_TextChanged" />
            </Grid>
        </Grid>
        <Border Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="12,0,12,4">
            <Grid>
                <TabView x:Name="MainTabView" 
                         AddTabButtonClick="MainTabView_AddTabButtonClick" 
                         HorizontalAlignment="Stretch" 
                         VerticalAlignment="Stretch">
                    <TabView.Resources>
                        <SolidColorBrush x:Key="TabViewBackground" Color="Transparent"/>
                    </TabView.Resources>
                    <TabViewItem Header="Vanara Jñāna">
                        <Frame x:Name="MainFrame" CornerRadius="8"/>
                    </TabViewItem>
                </TabView>
            </Grid>
        </Border>
        <controls:FloatingStatusBar Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Stretch"/>
    </Grid>
</Page>
