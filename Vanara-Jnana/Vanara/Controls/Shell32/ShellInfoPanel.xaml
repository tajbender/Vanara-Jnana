<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Jnana.Vanara.Controls.Shell32.ShellInfoPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Jnana.Vanara.Controls.Shell32"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,*,Auto" Padding="16">

        <!-- Header: Icon + Name + Type -->
        <StackPanel Orientation="Horizontal" Spacing="12">
            <Image
                x:Name="ItemIcon"
                Width="32"
                Height="32"
                Stretch="Uniform" />

            <StackPanel>
                <TextBlock
                    x:Name="ItemName"
                    FontSize="18"
                    FontWeight="SemiBold"
                    Text="Item Name" />

                <TextBlock
                    x:Name="ItemType"
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="File Type" />
            </StackPanel>
        </StackPanel>

        <!-- Properties Grid -->
        <ScrollViewer Grid.Row="1" Margin="0,16,0,0">
            <StackPanel Spacing="12">

                <Expander Header="Allgemein" IsExpanded="True">
                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto,Auto,Auto" 
                          RowSpacing="4" ColumnSpacing="12">

                        <TextBlock Text="Pfad:" Grid.Row="0" />
                        <TextBlock x:Name="ItemPath" Grid.Row="0" Grid.Column="1" />

                        <TextBlock Text="Größe:" Grid.Row="1" />
                        <TextBlock x:Name="ItemSize" Grid.Row="1" Grid.Column="1" />

                        <TextBlock Text="Erstellt:" Grid.Row="2" />
                        <TextBlock x:Name="ItemCreated" Grid.Row="2" Grid.Column="1" />

                        <TextBlock Text="Geändert:" Grid.Row="3" />
                        <TextBlock x:Name="ItemModified" Grid.Row="3" Grid.Column="1" />
                    </Grid>
                </Expander>

                <Expander Header="Attribute" IsExpanded="False">
                    <ItemsControl x:Name="AttributeList" />
                </Expander>

                <Expander Header="Shell Properties" IsExpanded="False">
                    <ItemsControl x:Name="ShellPropertyList" />
                </Expander>

                <Expander Header="Sicherheit" IsExpanded="False">
                    <ItemsControl x:Name="SecurityList" />
                </Expander>

                <Expander Header="Hash-Werte" IsExpanded="False">
                    <StackPanel Spacing="4">
                        <TextBlock x:Name="HashMD5" />
                        <TextBlock x:Name="HashSHA1" />
                        <TextBlock x:Name="HashSHA256" />
                    </StackPanel>
                </Expander>

                <Expander Header="Alternate Data Streams" IsExpanded="False">
                    <ItemsControl x:Name="AdsList" />
                </Expander>

                <Expander Header="Link-Ziel" IsExpanded="False">
                    <TextBlock x:Name="LinkTarget" />
                </Expander>

            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button Content="Öffnen" />
            <Button Content="Im Explorer anzeigen" />
            <Button Content="Aktualisieren" />
        </StackPanel>

    </Grid>
</UserControl>
