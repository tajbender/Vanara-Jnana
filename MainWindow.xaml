<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Jnana.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:nav="using:Jnana.Core.Navigation"
    xmlns:ctrls="using:Jnana.Workbench.Controls"
    mc:Ignorable="d"
    Title="Vanara Jñāna">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- QuickLaunchBar -->
        <ctrls:QuickLaunchBar x:Name="QuickLaunchBar" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Stretch"/>
        <TitleBar Grid.Column="1" Grid.Row="0"
                  IsBackButtonEnabled="{x:Bind IsBackButtonEnabled, Mode=OneWay}"
                  IsBackButtonVisible="{x:Bind IsBackButtonVisible, Mode=OneWay}"
                  Subtitle="{x:Bind SubtitleText, Mode=OneWay}" 
                  Title="{x:Bind TitleText, Mode=OneWay}" >
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Square44x44Logo.png" />
            </TitleBar.IconSource>
        </TitleBar>
        <!-- Navigation Host -->
        <nav:NavigationHost x:Name="NavigationHost" Grid.Column="1" Grid.Row="1" VerticalAlignment="Stretch"/>
    </Grid>
</Window>
