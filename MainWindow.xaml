<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Jnana.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Jnana"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:nav="using:Jnana.Core.Navigation"
    mc:Ignorable="d"
    Title="Vanara Jnana">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="MainHostGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="256" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="256" />
            </Grid.ColumnDefinitions>
            <nav:NavigationHost x:Name="PrimarySidebarHost" Grid.Column="0"/>
            <nav:NavigationHost x:Name="MainWindowHost" Grid.Column="1"/>
            <nav:NavigationHost x:Name="PrimaryLeftSidebarHost" Grid.Column="2"/>
        </Grid>

        <Grid x:Name="SecondaryPaneHostGrid" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="256" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="256" />
            </Grid.ColumnDefinitions>
            <nav:NavigationHost x:Name="SecondarySidebarHost" Grid.Column="0"/>
            <nav:NavigationHost x:Name="SecondaryWindowHost" Grid.Column="1"/>
            <nav:NavigationHost x:Name="SecondaryRightSidebarHost" Grid.Column="2"/>
        </Grid>

        <Grid x:Name="BottomStatusBarHostGrid" Grid.Row="1"/>

    </Grid>
</Window>
