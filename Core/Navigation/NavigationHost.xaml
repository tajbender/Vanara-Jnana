<?xml version="1.0" encoding="utf-8"?>

<UserControl
    x:Class="Jnana.Core.Navigation.NavigationHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" BorderBrush="{ThemeResource SystemControlTransparentRevealBorderBrush}" BorderThickness="3">
            <TreeView />
        </Border>
        <Border Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="0" BorderBrush="{ThemeResource SystemControlTransparentRevealBorderBrush}" BorderThickness="3">
            <TextBlock>NavigationBar will be placed here.</TextBlock>
        </Border>
        <Border Grid.Column="1" Grid.Row="1"
                Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                BorderBrush="{ThemeResource SystemControlTransparentRevealBorderBrush}"
                BorderThickness="3" CornerRadius="32" Margin="12">
            <TabView AddTabButtonClick="TabView_AddButtonClick" TabCloseRequested="TabView_TabCloseRequested">
                <TabView.TabItems>
                    <TabViewItem Header="Document 0">
                        <TabViewItem.IconSource>
                            <SymbolIconSource Symbol="Placeholder" />
                        </TabViewItem.IconSource>
                        <!--<samplepages:SamplePage1 />-->
                    </TabViewItem>
                    <TabViewItem Header="Document 1">
                        <TabViewItem.IconSource>
                            <SymbolIconSource Symbol="Placeholder" />
                        </TabViewItem.IconSource>
                        <!--<samplepages:SamplePage2 />-->
                    </TabViewItem>
                    <TabViewItem Header="Document 2">
                        <TabViewItem.IconSource>
                            <SymbolIconSource Symbol="Placeholder" />
                        </TabViewItem.IconSource>
                        <!--<samplepages:SamplePage3 />-->
                    </TabViewItem>
                </TabView.TabItems>
            </TabView>

            <!-- TODO: Implement navigation host -->
            <!-- <Frame x:Name="RootFrame" NavigationUIVisibility="Hidden"/> -->
            <!--<ContentPresenter x:Name="MainAreaPresenter" />-->

            <!--<controls:AttachDropShadow BlurRadius="16" ShadowOpacity="0.35" OffsetX="0" OffsetY="4"> -->
        </Border>
        <Border Grid.Column="2" Grid.Row="1" 
                BorderBrush="{ThemeResource SystemControlTransparentRevealBorderBrush}"
                BorderThickness="3">
            <!--<VariableSizedWrapGrid>
                <ContentPresenter x:Name="SideBarPresenter" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </VariableSizedWrapGrid>-->
        </Border>

        <!--<Border Grid.Column="1" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" BorderBrush="{ThemeResource SystemControlTransparentRevealBorderBrush}" BorderThickness="3" CornerRadius="32" Margin="12" > -->
        <!--<controls:DropShadowPanel BlurRadius="16" ShadowOpacity="0.35" OffsetX="0" OffsetY="4"> -->
        <!-- <ContentPresenter x:Name="SideBarPresenter" /> </Border>-->
    </Grid>
</UserControl>